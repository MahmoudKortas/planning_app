import 'package:flutter/material.dart';

Widget Info() {
  return const Center(
    child: Text(
      "Search results are displayed here",
      style: TextStyle(
        fontSize: 20,
        color: Colors.grey,
      ),
    ),
  );
}
